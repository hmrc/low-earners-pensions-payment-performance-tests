# Copyright 2023 HM Revenue & Customs
#

# This file is read when runLocal = true in application.conf (default)

# For local development convenience, host and protocol will have the following defaults:
#      protocol = http
#      host = localhost

# Example 1: (the base url for helloworld-service is https://127.0.0.1:9000)
# helloworld-service {
#      protocol = https
#      host = 127.0.0.1
#      port = 9000
# }

# Example 2: (the base url for helloworld-service is http://localhost:9000)
# helloworld-service.port = 9000


services {
  low-earners-pensions-payment-frontend-frontend.port = 30029
  auth-login-stub.port= 9949

low-earners-pensions-payment-frontend-frontend{
  protocol = http
  host = localhost
  port = 30029
  }

  auth-login-stub {
    protocol = http
    host = localhost
    port = 9949
  }

  base-url {
    protocol = http
    host = localhost
    port = 30029
  }
}
